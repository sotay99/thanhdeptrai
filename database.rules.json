{
  "rules": {
    ".read": false,
    ".write": false,

    "thongtinthanhtoan": {
      "//": "Thông tin chuyển khoản: cả web đọc công khai (khách không đăng nhập). GHI thì chỉ hai email của chủ shop, qua trang /admin. Đây là quyền THẬT — giao diện admin chỉ là lớp lịch sự bên ngoài.",
      ".read": true,
      ".write": "auth != null && auth.token.email_verified === true && (auth.token.email === 'lookatmevanthanhpham@gmail.com' || auth.token.email === '219thanhdeptrai@gmail.com')"
    },

    "thongtinlienhe": {
      "//": "Số Zalo của shop và các link mạng xã hội: web đọc công khai để dựng các nút, chỉ chủ shop ghi được. Giữ ở đây thay vì trong mã nguồn để số không lộ ra trên GitHub.",
      ".read": true,
      ".write": "auth != null && auth.token.email_verified === true && (auth.token.email === 'lookatmevanthanhpham@gmail.com' || auth.token.email === '219thanhdeptrai@gmail.com')"
    },

    "admin": {
      "//": "Cài đặt riêng tư của chủ shop. KHÁC hẳn ba nhánh trên: nhánh này CẤM ĐỌC công khai. Khoá API của AI nằm trong đây, và khoá đó không bao giờ được đi qua trình duyệt khách — chatbot hỏi Worker, Worker mới giữ khoá và gọi AI. Worker đọc bằng khoá dịch vụ riêng của nó, không qua rules này.",
      ".read": "auth != null && auth.token.email_verified === true && (auth.token.email === 'lookatmevanthanhpham@gmail.com' || auth.token.email === '219thanhdeptrai@gmail.com')",
      ".write": "auth != null && auth.token.email_verified === true && (auth.token.email === 'lookatmevanthanhpham@gmail.com' || auth.token.email === '219thanhdeptrai@gmail.com')",
      "khoaAI": {
        "khoa": { ".validate": "newData.isString() && newData.val().length <= 300" },
        "nhaCungCap": { ".validate": "newData.isString() && newData.val().length <= 40" },
        "model": { ".validate": "newData.isString() && newData.val().length <= 80" },
        "$khac": { ".validate": false }
      }
    },

    "danhmuc": {
      "//": "Tên hiển thị của từng sản phẩm và từng file trong R2. Web của khách đọc để bày danh sách, chỉ chủ shop ghi.",
      ".read": true,
      ".write": "auth != null && auth.token.email_verified === true && (auth.token.email === 'lookatmevanthanhpham@gmail.com' || auth.token.email === '219thanhdeptrai@gmail.com')"
    },

    "thongtinkho": {
      "//": "Địa chỉ máy chủ cấp phát đường dẫn tải sản phẩm. Web đọc công khai để trang /sanpham hỏi được, chỉ chủ shop ghi. Bản thân đường dẫn tới từng file KHÔNG nằm ở đây — chỉ máy chủ cấp phát mới biết, và nó chỉ trả về khi mã nhận hàng hợp lệ.",
      ".read": true,
      ".write": "auth != null && auth.token.email_verified === true && (auth.token.email === 'lookatmevanthanhpham@gmail.com' || auth.token.email === '219thanhdeptrai@gmail.com')"
    },

    "donhang": {
      "//": "Đơn hàng: cho TẠO MỚI và cho cập nhật cờ xác nhận, nhưng KHÔNG cho đọc — khách này không xem được đơn của khách khác. Chủ shop xem trong Firebase Console.",
      "//2": "indexOn trangThai để khâu gửi hàng tự động lọc nhanh những đơn đang chờ gửi, thay vì tải cả kho đơn về.",
      "//3": "Khách KHÔNG đọc được đơn của ai cả; chỉ hai email chủ shop đọc được, để trang /admin bày danh sách đơn.",
      ".read": "auth != null && auth.token.email_verified === true && (auth.token.email === 'lookatmevanthanhpham@gmail.com' || auth.token.email === '219thanhdeptrai@gmail.com')",
      ".indexOn": ["trangThai"],
      "$madon": {
        ".read": "auth != null && auth.token.email_verified === true && (auth.token.email === 'lookatmevanthanhpham@gmail.com' || auth.token.email === '219thanhdeptrai@gmail.com')",
        ".write": true,
        ".validate": "newData.hasChildren(['sanPham', 'thanhTien', 'taoLuc'])",
        "sanPham": { ".validate": "newData.hasChildren()" },
        "tongTien": { ".validate": "newData.isNumber() && newData.val() >= 0" },
        "phanTramGiam": { ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 100" },
        "thanhTien": { ".validate": "newData.isNumber() && newData.val() >= 0" },
        "maSanPham": { ".validate": "newData.hasChildren()" },
        "trangThai": { ".validate": "newData.isString() && newData.val().length <= 20" },
        "maDon": { ".validate": "newData.isString() && newData.val().length <= 12" },
        "maNhanHang": { ".validate": "newData.isString() && newData.val().length <= 32" },
        "guiLuc": { ".validate": "newData.isNumber()" },
        "ghiChuGui": { ".validate": "newData.isString() && newData.val().length <= 300" },
        "email": { ".validate": "newData.isString() && newData.val().length <= 35" },
        "zalo": { ".validate": "newData.isString() && newData.val().length <= 13" },
        "dienThoai": { ".validate": "newData.isString() && newData.val().length <= 13" },
        "noiDungCK": { ".validate": "newData.isString() && newData.val().length <= 120" },
        "taoLuc": { ".validate": "newData.isNumber()" },
        "daXacNhan": { ".validate": "newData.isBoolean()" },
        "xacNhanLuc": { ".validate": "newData.isNumber()" },
        "$khac": { ".validate": false }
      }
    }
  }
}
